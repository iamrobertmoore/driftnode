# n8n-nodes-vultr

This package is **generated output**. It is produced by `driftnode` from Vultr's
published API documentation and is committed here so judges can inspect exactly what
the generator emits, and diff it against a regeneration.

Do not hand-edit files in this package. Change the spec or the generator and regenerate.
