/**
 * driftnode
 *
 * Scaffold only. The generator implementation is produced by Kiro from the
 * specs in .kiro/specs and replaces the contents of this directory.
 */
export const PACKAGE_NAME = 'driftnode';

export function version(): string {
  return '0.1.0';
}
