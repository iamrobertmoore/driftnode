node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
*.log

# The .kiro directory is a required submission artefact and is deliberately
# NOT ignored here. Do not add it. See README, "How I used Kiro".

# Temporary directories created during generation
.tmp-*/

# Asset folders produced by a browser "Save Page As, Complete" save.
# Only the .html snapshot itself belongs in the repo.
*_files/
